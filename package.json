{
  "name": "wmsports-mock-server",
  "version": "2.0.0",
  "description": "Microcks-powered mock server for WM Sports — GraphQL, REST, Kafka, RabbitMQ with a dashboard UI",
  "private": true,
  "scripts": {
    "start": "node server.cjs",
    "dev": "node server.cjs",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:import": "docker compose run --rm import"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
